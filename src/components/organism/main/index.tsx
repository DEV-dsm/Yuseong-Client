import React from 'react';
import * as _ from './style';

/** @returns 메인 component */
export default function MainCompo() {
    return (
        <_.Container>
            <_.Wrapper></_.Wrapper>
        </_.Container>
    );
}
